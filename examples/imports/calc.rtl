# A small math helper module imported by main.rtl.

fn add(int a, int b) gives int {
    give a + b;
}

fn multiply(int a, int b) gives int {
    give a * b;
}

fn square(int n) gives int {
    give n * n;
}

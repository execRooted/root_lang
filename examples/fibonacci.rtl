use("../stdlib/root.rtl") aka std;

fn fib(int n) gives int {
    when (n <= 1) {
        give n;
    }
    give fib(n - 1) + fib(n - 2);
}

fn main() gives void {
    walk (int i = 0, i < 10, i += 1) {
        std.emit(fib(i));
        std.emit(" ");
    }
    std.line();
}
